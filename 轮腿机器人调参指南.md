# 轮腿机器人调参指南

## 10 维 LQR + PID 力控 + VMC 映射框架参数整定手册

**编写说明：**

本文档是《轮腿机器人控制理论手册》的配套工程文档，聚焦于参数整定的理论依据、数学推导、数值计算与验证闭环。本文假设读者已掌握状态空间建模、LQR 最优控制和 PID 控制的基础知识，并已根据自身平台完成机械参数测定、运动学解算验证和传感器标定。

本文档的目标是提供一份平台无关、面向所有轮腿机器人构型的通用调参方法论。具体数值以五连杆串联腿、10 维状态空间为例，但方法论适用于串联/并联、10 维/6 维等所有常见构型。

**安全声明：** 本文档给出的初始值计算方法和整定流程仅作为设计起点。所有参数必须在仿真中充分验证后方可移植至实机，实机首次测试应从最低增益开始，逐步递增。

---

## 目录

1. [参数体系总览](#1)
2. [LQR Q/R 权重矩阵设计](#2)
3. [K 矩阵结构分析与控制策略解读](#3)
4. [PID 力控参数整定](#4)
5. [参考调速器设计](#5)
6. [安全限幅参数](#6)
7. [增益调度](#7)
8. [分阶段整定路线图](#8)
9. [K 矩阵诊断与交叉验证](#9)
10. [常见异常与排查](#10)


---

## 1. 参数体系总览

轮腿机器人的可调参数分布于三个层级，存在明确的数据流向和生效路径：

```
离线工具链（线性化 + DARE 求解）
  Q_DIAG (R^10):   各状态的偏差代价
  R_MAT  (R^4x4):  各控制通道的能量代价
  DT:               ZOH 离散化步长
  H_MIN, H_MAX:     增益调度腿长范围
  D_d:              未建模阻尼补偿
    |
    | DARE 求解
    v
  K 增益表 (4x10 矩阵随腿长变化)
  e2(L_l, L_r):     俯仰平衡偏移
  e3(L_l, L_r):     腿角平衡偏移
  u_eq(L_l, L_r):   平衡控制量（重力补偿前馈）
    |
    | 离线生成，在线查表
    v
在线控制层（实时控制循环）
  leg_length_pid:    腿长位置/速度级联 PID
  roll_pid:          Roll 稳定 PID
    |
    v
安全层（限幅/保护）
  joint_torque_limit:        关节力矩硬限幅
  wheel_torque_limit:        轮毂力矩硬限幅
  length_position_force_limit: 腿长位置环力限幅
  length_velocity_force_limit: 腿长速度环力限幅
  roll_force_limit:          Roll 力限幅
  fall_angle_rad:            倾倒保护角
```

### 参数变更的生效路径

| 参数类别 | 变更后需要 | 生效方式 |
|---------|-----------|---------|
| Q_DIAG / R_MAT | 重新运行线性化工具，生成新的 .npy/.h 系数文件 | 新的 K/e2/e3/u_eq 查表生效 |
| DT / H_MIN / H_MAX | 重新运行线性化 | 影响 Ad/Bd 离散化和调度范围 |
| PID 参数 (kp, ki, kd) | 重新编译/重启 | 在线读取，实时生效 |
| 安全限幅参数 | 重新启动 | 在控制链末端生效 |
| 调速器参数 | 无需重启 | 通过控制器在线修改目标值 |

### 参数间耦合关系

- **Q/R -> K -> 控制力矩**：Q/R 比值变化直接改变 K 的 40 个元素，进而改变各通道反馈强度。力矩是否触及限幅由安全层决定。
- **PID Kp 过大 -> 线性化假设失效**：腿长 PID 的 Kp 过大，腿伸缩过快，质心高度快速变化，俯仰通道的有效惯量和重力刚度快速变化，LQR 的线性化假设被违背。
- **Kalman q_vel 过大 -> LQR 有效阻尼上限降低**：速度估计噪声放大 -> LQR 的 x_dot 通道含高频噪声 -> 轮毂力矩抖动 -> 轮地接触力波动 -> 打滑风险。
- **轮毂前馈过大 -> 打滑 -> 里程计失效**：轮加速度超过静摩擦上限 -> 打滑 -> 里程计不可信 -> 位移估计漂移 -> 位置跟踪发散。


---

## 2. LQR Q/R 权重矩阵——从物理约束到数值求解

### 2.1 代价函数的物理结构

LQR 求解如下无穷时域二次型代价函数的最小化问题：

$$J = \int_0^\infty (x^T Q x + u^T R u) dt$$

在 (A,B) 可稳定、(A,Q^{1/2}) 可检测条件下，最优解具有线性状态反馈形式：

$$u = -K x, \quad K = R^{-1} B^T P$$

其中 P 为连续代数 Riccati 方程（CARE）的唯一对称正定解：

$$A^T P + P A - P B R^{-1} B^T P + Q = 0$$

数字实现中，先做 ZOH 离散化 (A,B) -> (Ad,Bd)，再解离散 Riccati 方程（DARE）：

$$P = A_d^T P A_d - A_d^T P B_d (R + B_d^T P B_d)^{-1} B_d^T P A_d + Q$$

$$K = (R + B_d^T P B_d)^{-1} B_d^T P A_d$$

**Q 和 R 的物理量纲：**

- Q 的对角元 Q_ii：对第 i 个状态偏离的惩罚。量纲 = [代价]/[x_i^2]。
- R 的对角元 R_jj：对第 j 个控制通道幅值的惩罚。量纲 = [代价]/[u_j^2]。
- **Q/R 的相对比值决定 K，同比例缩放不影响 K**。若 (Q,R) -> (aQ,aR)，则 P -> aP，K = (aR + B^T(aP)B)^{-1} B^T(aP)A = (R + B^T P B)^{-1} B^T P A 不变。

### 2.2 量纲归一化——Brysons 法则

**问题**：位移 (m)、角度 (rad)、角速度 (rad/s) 和控制力矩 (Nm) 具有不同的物理量纲，无法在同一代价函数中直接比较。Brysons 法则将各通道归一化为无量纲的"偏差比例"：

$$Q_{ii}^{(0)} = \frac{1}{(\Delta x_i^{\max})^2}, \quad R_{jj}^{(0)} = \frac{1}{(u_j^{\max})^2}$$

其中 dxi_max 是第 i 个状态的最大可接受偏差，u_j_max 是第 j 个控制通道的**设计可用力矩**（Riccati 方程中的"软限幅"，通常取硬件限幅的 30-50%，留安全余量）。

#### 各状态最大可接受偏差的确定

| 状态 | 量纲 | 确定方法 | 参考区间 |
|------|------|---------|---------|
| theta (俯仰角) | rad | 物理倾倒角的 1/5 ~ 1/3 | 倾倒角 20-30 deg，取 4-6 deg (0.07-0.10 rad) |
| theta_L, theta_R (腿摆角) | rad | 腿工作空间的 1/5 | 工作范围约 30 deg，取 6 deg (0.10 rad) |
| x (水平位移) | m | 允许的最大稳态跟踪误差 | 0.3 m |
| psi (偏航角) | rad | 允许的最大航向偏差 | 0.4 rad |
| 速度状态 | — | 不遵循 Brysons，见 2.4 节 | — |

示例计算：取 dx_theta = 0.10 rad -> Q_theta = 1/(0.10^2) = 100。
取 dx_theta_L = 0.10 rad -> Q_leg = 100。
取 dx_x = 0.30 m -> Q_x = 11。
取 dx_psi = 0.40 rad -> Q_psi = 6。

#### 各控制通道设计可用力矩的确定

| 控制量 | 确定方法 | 示例（关节 40Nm/轮毂 5Nm） |
|--------|---------|--------------------------|
| tau_L, tau_R (髋力矩) | 取关节硬限幅的 30-50% | 硬限幅 40 Nm，取 15 Nm -> R_hip = 1/225 = 0.0044 |
| T_wL, T_wR (轮毂力矩) | 取轮毂硬限幅的 30-50% | 硬限幅 5 Nm，取 2 Nm -> R_wheel = 1/4 = 0.25 |

**为什么工程值通常远大于 Brysons 值？** Brysons 法则是"量纲归一化"的起点，给出的 R 值（如 R_hip=0.004）在工程中极少直接使用。原因：(1) Brysons 仅保证各通道量级可比，不保证整体控制激进程度合适；(2) 工程中需乘以全局缩放因子调节激进程度；(3) Q/R 比值才是决定 K 矩阵的核心，R 的具体绝对值不重要。实际调试中，工程师通常在 Brysons 基础上将 R 放大 10-100 倍作为保守起点，再逐步降低。


### 2.3 Q 矩阵的位置权重设计

位置权重（状态 x, psi, theta, theta_L, theta_R）遵循"重要性分层"原则：

**第一优先级——俯仰角 theta（不稳定模态）**

俯仰角是倒立摆的不稳定模态——开环 A 矩阵存在正实部特征值（鞍点不稳定性）。如果 Q_theta 不够大，闭环极点可能无法被充分拽入单位圆内。调节标准：
- 闭环动态极点（排除 x/psi 积分器后）的最大模应 < 0.97
- 时域俯仰阶跃响应（初始 theta=0.05 rad）应在 0.3-0.8s 内回到 5% 以内

典型取值范围：Q_theta = 5000 ~ 20000。

**第二优先级——腿摆角 theta_L, theta_R（质心调节）**

腿摆角直接影响质心位置和俯仰稳定性。腿角权重决定了 LQR 对腿角偏差的响应强度，同时对腿的"软硬"感受有直接贡献：
- Q_leg 大 -> 腿"硬"，飞坡压缩量小，落地冲击力大
- Q_leg 小 -> 腿"软"，飞坡缓冲充足，但静态站立可能发软

典型取值范围：Q_leg = 1000 ~ 8000。

**第三优先级——水平位移 x（欠驱动通道）**

位移是欠驱动通道——LQR 无法直接施加 x 方向的控制力，只能通过俯仰偏转间接产生水平加速度。Q_x 过大会导致 LQR 为追求位移收敛而过度倾斜机身。
- 主要作用：提供一个"目标方向"，在满足俯仰稳定约束下尽量减小位移偏差
- Q_x / Q_theta 比值通常 < 0.01

**第四优先级——偏航角 psi（近似解耦通道）**

偏航与俯仰近似解耦，偏航控制在姿控稳定后再调整。Q_psi 取较小值避免偏航控制与姿态控制竞争。

### 2.4 Q 矩阵的速度权重设计——阻尼整形

速度权重不遵循 Brysons 法则。它们通过 Riccati 方程中的交叉耦合影响各模态的阻尼比。

**俯仰角速度 theta_dot（最重要）**

决定俯仰模态的阻尼比 zeta。整定方法：
1. 保持 Q_theta 不变，Q_dtheta 从 0 开始逐步增大
2. 观察闭环特征值中俯仰模态的阻尼比变化
3. 增到刚好消除超调为止（临界阻尼附近，zeta = 0.7-0.9）
4. Q_dtheta / Q_theta 的典型比值：0.001 ~ 0.01

**水平速度 x_dot（刹车距离控制）**

决定速度跟踪的激进程度和刹车距离。这是影响驾驶体验的关键权重：
- Q_xdot 大 -> K 矩阵中轮力矩对速度误差的增益 K[2:4,5] 增大 -> 加速/刹车更猛
- Q_xdot 小 -> 加速柔和，但刹车距离变长
- 刹车距离直接受 Q_xdot 和轮力矩限幅共同控制

整定方法：从 Q_xdot = 1-5 开始，逐步增大直到加减速响应满意，同时监控是否出现速度超调振荡。

**腿摆角速度 theta_L_dot, theta_R_dot（腿运动阻尼）**

抑制腿在高增益位置反馈下的高频抖动。初值取 Q_dtheta 的 0.5-1 倍，逐步增大直到腿摆动平滑无抖动。

**偏航角速度 psi_dot**

抑制偏航振荡。初值取 Q_psi 的 1-5 倍。

### 2.5 R 矩阵与控制分配策略

R 矩阵的内部比值 R_hip / R_wheel 决定了 LQR 对髋关节力矩 vs 轮毂力矩的偏好。这是影响"驾驶风格"的关键设计参数。

**物理基础：控制通道的有效性差异**

不同控制通道对同一个状态的影响效率不同。以 x 方向加速度为例：

- 轮毂力矩 -> x 加速度：高效。T_w 通过轮地摩擦直接产生 x 方向力，B_d[5,2:4] 较大。
- 髋关节力矩 -> x 加速度：间接。需要先倾斜身体产生重力分量，B_d[5,0:2] 远小于轮毂通道。

同样，俯仰角加速度只能通过髋力矩产生（B_d[7,2:4] = 0，轮毂力矩无俯仰耦合）。

**控制分配策略与效果**

| R_hip / R_wheel | LQR 偏好 | 驾驶感受 | 适用场景 |
|-----------------|---------|---------|---------|
| < 1 (R_hip 更小) | 多用髋力矩 | 身体主动前倾/后仰，更像 Segway | 需要身体前倾场景 |
| > 1 (R_hip 更大) | 多用轮毂力矩 | 身体保持直立，轮子直接加减速 | 高速巡航、快速刹车 |
| R_hip << R_wheel | 极限用髋 | 大幅身体摆动，可能有振荡风险 | 特殊机动（倒地自起） |
| R_hip >> R_wheel | 极限用轮 | 身体几乎不动，轮子承担全部加减速 | — |

**分析 K 矩阵中当前的控制分配**：计算轮力矩和髋力矩分别产生的有效 x 加速度：

```python
# B_cont = Bd / dt  (连续时间 B 矩阵)
leg_x_accel = B_cont[5,0]*K[0,5] + B_cont[5,1]*K[1,5]      # 髋力矩 -> x 加速度
wheel_x_accel = B_cont[5,2]*K[2,5] + B_cont[5,3]*K[3,5]    # 轮力矩 -> x 加速度
# 两者比值反映当前策略
```

轮力矩的 x 加速效率通常是髋力矩的 5-20 倍（由 B 矩阵物理决定），因此即使 R_hip < R_wheel，轮力矩在 x 加速中仍占主导。髋力矩的核心价值在于产生俯仰力矩实现身体倾斜。

**整定建议**：
- 初始取 R_hip = R_wheel（中性分配）
- 若希望加速时身体主动前倾：降低 R_hip（如 0.3-0.6 相对 R_wheel=1）
- 若希望急刹车时身体后仰协助制动：同上（松手后 tau 产生后仰力矩 -> 重力制动）
- 实机上 R_hip 通常小于 R_wheel，因为关节电机力矩容量远大于轮毂电机

### 2.6 DARE 求解与 K 矩阵验证

DARE 的标准求解方法有两种：

1. **Schur 分解法**（推荐）：数值稳定，无收敛问题。对应 `scipy.linalg.solve_discrete_are(Ad, Bd, Q, R)`。
2. **反向迭代法**（备用）：从终端代价 S 出发反向递推。在 Schur 法失败时使用。

验证标准：
- 闭环极点：max|eig(A_d - B_d*K)| < 0.99（必须，否则不稳定）
- 排除积分器（x, psi 通道）后的动态极点：< 0.97
- K 矩阵最大值 < 5000（超出说明 Q/R 比值过大，反馈增益过高）
- K 矩阵不应有任何元素精确为零


---

## 3. K 矩阵结构分析与控制策略解读

### 3.1 K 矩阵的分块物理含义

K 是 4x10 矩阵，将 10 维状态偏差映射为 4 维控制指令 u = -K*(x - x_ref)：

```
控制量 (行):  [tau_L, tau_R, T_wL, T_wR]
状态量 (列):  [x, psi, theta, theta_L, theta_R, x_dot, psi_dot, theta_dot, thetaL_dot, thetaR_dot]
```

**关键子块的物理含义：**

| K 元素 | 物理含义 | 对驾驶感受的影响 |
|--------|---------|----------------|
| K[0:2,0] | 髋力矩对位移误差的响应 | 位移跟踪的"回拉"感 |
| K[2:4,0] | 轮力矩对位移误差的响应 | 位移跟踪的轮上响应 |
| K[0:2,2] | 髋力矩对俯仰角的响应 | 俯仰恢复强度，姿态"硬"度 |
| K[2:4,2] | 轮力矩对俯仰角的响应 | 俯仰->轮力矩交叉耦合 |
| K[0:2,3:5] | 髋力矩对腿角的响应 | 腿角刚度的 LQR 部分 |
| K[0:2,5] | 髋力矩对速度误差的响应 | 加速时身体前倾强度 |
| K[2:4,5] | 轮力矩对速度误差的响应 | **刹车距离的决定性因素** |

### 3.2 加速/减速通道的轮-腿分配

轮力矩和髋力矩对 x 加速度的有效性不同。通过连续时间 B 矩阵（B_cont = B_d / dt）将 K 增益转化为实际物理效果：

```python
B_cont = B_d / dt
# 髋力矩 -> x 加速度 (m/s^2 per Nm)
leg_to_accel = B_cont[5,0:2]
# 轮力矩 -> x 加速度 (m/s^2 per Nm)
wheel_to_accel = B_cont[5,2:4]

# 速度误差 1 m/s 时的制动力分解：
leg_brake = leg_to_accel[0]*K[0,5] + leg_to_accel[1]*K[1,5]
wheel_brake = wheel_to_accel[0]*K[2,5] + wheel_to_accel[1]*K[3,5]
```

**物理分工：**
- **轮力矩 -> 直接 x 加速度**：效率高（B_cont[5,2:4] 较大），是加减速的主力（通常占比 > 90%）
- **髋力矩 -> 身体俯仰 -> 重力分量加速度**：效率低但不可替代，因为只有髋力矩能产生俯仰力矩（B_cont[7,2:4] = 0，轮力矩无俯仰耦合）

这导致一个关键设计约束：**LQR 不能为了加速效率而只使用轮力矩**，因为俯仰稳定完全依赖髋力矩。

### 3.3 俯仰稳定性的增益依赖

俯仰通道是轮腿机器人控制的核心——俯仰角是不稳定模态，如果 K 矩阵中与俯仰相关的增益不足，系统会迅速发散。

**俯仰控制的三条增益路径：**

1. K[0:2,2]：髋力矩直接响应俯仰角偏差 -> 产生俯仰恢复力矩
2. K[2:4,2]：轮力矩交叉响应俯仰角偏差 -> 辅助俯仰恢复（但无直接俯仰力矩，通过改变轮地接触力间接作用）
3. K[0:2,7]：髋力矩响应俯仰角速度 -> 提供俯仰阻尼

**俯仰增益不足的征兆：**
- 闭环极点中俯仰模态的阻尼比 < 0.3
- 站立时受轻微扰动后多次振荡才能恢复
- 加速/减速时俯仰角大幅摆动（> 0.1 rad）

**整定顺序：** 先调 Q_theta 确保俯仰有足够恢复力，再调 Q_dtheta 增加阻尼消除振荡。


---

## 4. PID 力控参数整定

PID 力控负责在任务空间（虚拟腿长 L、Roll 角）产生轴向力指令。PID 与 LQR 的分工原则：PID 处理可解耦的单通道任务（腿长伸缩、Roll 稳定），LQR 处理全耦合的多通道协调（姿态平衡 + 位移跟踪）。

### 4.1 腿长位置-速度级联 PID

**结构**：外环（位置 P）+ 内环（速度 P），无积分项：

```
F_pos = Kp_pos * (L_target - L_measured)
F_vel = Kp_vel * (0 - L_dot_measured)
F_leg = F_pos + F_vel + G_m * cos(theta_leg)
```

**不使用积分项 (Ki=0) 的原因**：轮腿机器人的腿长变化频繁（地形适应、飞坡落地、跨越台阶），积分器的记忆效应会在腿长切换时引起过冲和滞后。稳态精度由 LQR 的位移积分效应和重力前馈补偿保证。

**参数含义：**

| 参数 | 物理含义 | 增大的效果 | 过大的风险 |
|------|---------|-----------|-----------|
| Kp_pos | 腿长偏差 -> 轴向回弹力 (N/m) | 腿长跟踪更紧，站立更"硬" | 飞坡压缩量过小，冲击力传导至机身；腿伸缩超过轮地摩擦 -> 打滑 |
| Kp_vel | 腿伸缩速度 -> 阻尼力 (N/(m/s)) | 伸缩更平滑，抑制振荡 | 腿伸缩阻力过大 -> 落地时腿无法缓冲 |

**初始值计算：**

- Kp_pos：从静态承载出发。腿长偏差 1cm 应产生足够额外力来加速腿部质量。
  Kp_pos^(0) = m_leg * g / 0.01。以 m_leg = 1.2 kg 为例：1.2*9.81/0.01 = 1177。取 1.5-2x 安全因子 -> 1500-2500。
- Kp_vel：取临界阻尼。Kp_vel^(0) = 2*sqrt(Kp_pos * m_leg) * zeta，zeta=0.7 -> 2*sqrt(2000*1.2)*0.7 = 68。取整到 50-150。

**整定流程**（在 LQR 已稳定后执行）：

1. 关闭 LQR（将 K 矩阵临时置零），仅保留 PID + 重力补偿
2. 调 Kp_pos：从 500 开始，每次 +500，直到腿长阶跃响应（目标腿长从 0.20m 跳到 0.25m）在 0.3s 内完成且无明显超调
3. 调 Kp_vel：从 50 开始，每次 +50，直到腿伸缩过程平滑无振荡
4. 开启 LQR，检查 PID 与 LQR 是否冲突：若腿长频繁振荡，降低 Kp_pos 20-30%

**特殊场景——飞坡缓冲**：若机器人飞坡落地压缩量太小、冲击感强：
1. 降低 Kp_pos（减少"弹簧刚度"）-> 500-1000
2. 降低 Q_leg（减少 LQR 腿角反馈强度）-> 1000-2000
3. 必要时降低腿长位置力限幅（leg_length_position_force_limit）-> 300-500N

### 4.2 Roll 稳定 PID

**结构**：PD（Kp + Kd），无积分：

```
F_roll = Kp_roll * (0 - roll_angle) + Kd_roll * (0 - roll_rate)
```

Roll 方向是自稳定的（重力提供恢复力矩），PID 提供额外阻尼和加快恢复。

**初始值计算**：
- Kp_roll^(0) = I_roll * (2*pi*f_roll)^2，I_roll 为绕 x 轴的有效转动惯量（约 total_mass * (avg_L/2)^2），f_roll 为自然振荡频率（约 2-4 Hz）
- Kp_roll 取 500-3000
- Kd_roll^(0) = 2*sqrt(Kp_roll * I_roll) * 0.7，取 200-1500

### 4.3 力控综合与重力前馈

最终的腿轴向力指令为四项之和：

```
F_left  = F_left_pos + F_left_vel + F_roll + G_m * cos(theta_L)
F_right = F_right_pos + F_right_vel - F_roll + G_m * cos(theta_R)
```

其中 G_m = 0.5 * total_mass * g（半车重力）。cos(theta_leg) 将重力沿腿方向分解。

重力前馈的作用：补偿静态重力载荷，使 PID 和 LQR 只需处理动态偏差，而不需持续输出大稳态力来对抗重力。这减小了控制指令的直流分量，降低了执行器发热。


---

## 5. 参考调速器设计

调速器将操纵杆输入（-1.0 ~ +1.0）映射为目标速度和位置，LQR 的目标状态来源于调速器的输出。

### 5.1 速度映射

最简形式——直接映射：

```
v_target = speed_axis * v_max
omega_target = yaw_axis * omega_max
```

可选斜坡限制（ramp rate limit）避免目标速度突变：

```
v_target(k+1) = v_target(k) + clip(v_desired - v_target(k), -a_max*dt, +a_max*dt)
```

其中 a_max 通常取轮地最大静摩擦加速度的 50%：a_max = 0.5 * mu * g。

### 5.2 刹车策略与位置目标管理

**核心问题**：松手瞬间，速度指令归零（v_target = 0），但位置目标是否同步更新？

**错误做法**——冻结位置目标：
松手时 position_target 保持在松手前积分到的位置。机器人靠 K[:,5]（速度增益）减速到零后，K[:,0]（位置增益）检测到"当前位置超出了目标位置"，命令轮子反转退回。表现为"停住后又回退"。

**正确做法**——松手瞬间 snap 位置目标：
```python
if v_target == 0 and prev_v_target != 0:
    position_target = current_position  # 消除位置误差
```
松手后 position_target 设成当前位置 -> 位置误差归零 -> K[:,0] 不产生回退指令 -> 纯靠 K[:,5] 刹车到停。

**刹车距离的决定因素**：
1. K[:,5] 速度增益的强度（由 Q_xdot 和 R_wheel 决定）
2. 轮力矩硬限幅（安全层参数）
3. 身体后仰 -> 重力制动（髋力矩 K[:,5] 决定后仰强度，由 Q_xdot 和 R_hip 比值决定）

刹车距离过长时的调参方向：增大 Q_xdot、降低 R_wheel、提高轮力矩限幅。


---

## 6. 安全限幅参数

### 6.1 倾倒保护角

| 参数 | 含义 | 整定方法 |
|------|------|---------|
| fall_angle_rad | 俯仰角或 Roll 角绝对值的临界值 | 取质心投影超出支撑域边界的临界姿态角的 0.8 倍 |

临界角 = atan(支撑半径 / 质心高度)。标称腿长下质心高度约 0.25m，支撑半径约 0.1m，临界角约 0.38 rad。取 0.8x 安全因子 -> 0.30 rad。但轮腿机器人可通过腿伸展扩大支撑域，实际可取更大值（0.5-1.0 rad）。

### 6.2 力矩与力限幅

| 参数 | 作用位置 | 典型范围 | 确定依据 |
|------|---------|---------|---------|
| joint_torque_limit | 控制链末端 clip | 20-60 Nm | 关节电机的额定/峰值力矩。参考电机手册 |
| wheel_torque_limit | 控制链末端 clip | 3-10 Nm | 轮毂电机的额定力矩。直接影响刹车距离 |
| length_position_force_limit | PID 位置环输出 | 500-3000 N | 防止腿长偏差大时产生危险轴向力 |
| length_velocity_force_limit | PID 速度环输出 | 500-2000 N | 防止腿伸缩过快时阻尼力过大 |
| roll_force_limit | Roll PID 输出 | 500-2000 N | 防止左右腿力差过大 |

力矩限幅是硬限幅（在控制链最末端 clip 操作），与 R 矩阵的"软权重"不同。R 告诉 LQR 在优化中尽量不碰限幅；硬限幅在优化结果超出物理能力时强制截断。

**注意：限幅 =/= 调节增益**。将限幅设得过低（如轮力矩限幅从 5 降到 2 Nm）不等于"调软"——它只是把控制指令削顶了，导致大误差时控制饱和、失去线性调节能力。减速/加速增益应通过 Q/R 调节，限幅仅防止超物理极限。


---

## 7. 增益调度——从单点到全工作空间

轮腿机器人的腿长在工作范围内连续变化，导致等效惯量、重力刚度和平衡点位置随之改变。单一标称腿长的 K 矩阵无法在全工作空间保持一致的性能。

### 7.1 平衡点随腿长的变化

在 (L_l, L_r) 非标称值时，系统平衡点不再是零姿态：

- e2(L_l, L_r)：机体俯仰角的平衡偏移。当左右腿不对称时，机体需倾斜以维持质心在支撑域中心。
- e3_l(L_l, L_r), e3_r(L_l, L_r)：左右腿摆角的平衡偏移。

这些偏移量在非线性动力学方程中令加速度为零求得（fsolve），并随 K 矩阵一起查表：
```
theta_ref = theta_target - e2(L_l, L_r)
theta_L_ref = theta_L_target - e3_l(L_l, L_r)
theta_R_ref = theta_R_target - e3_r(L_l, L_r)
```

### 7.2 拟合方法

**1D 调度（左右腿对称）**：K 仅为平均腿长 L = (L_l+L_r)/2 的函数，可用 1D 多项式拟合：
$$K_{ij}(L) = c_0 L^4 + c_1 L^3 + c_2 L^2 + c_3 L + c_4$$

**2D 调度（左右腿独立伸缩）**：K 为 (L_l, L_r) 的二元函数。推荐使用 2D 三角 Chebyshev 多项式拟合：
$$K_{ij}(L_l, L_r) = \sum_{d=0}^{p} \sum_{a=0}^{d} c_{da} \cdot T_{d-a}\left(\tilde{L}_l\right) \cdot T_a\left(\tilde{L}_r\right)$$

其中 $\tilde{L} = (2L - H_{\min} - H_{\max}) / (H_{\max} - H_{\min})$ 为归一化腿长，$T_k(x)$ 为第 k 阶 Chebyshev 多项式。

Chebyshev 拟合的优势：(1) 在逼近光滑函数时具有指数收敛速度；(2) 逼近误差在区间内近似均匀分布（near-minimax 性质）；(3) 8 阶 2D 三角 Chebyshev（45 项）通常能达到 < 0.1% 的相对 RMS 误差。

### 7.3 调度表的质量评估

| 指标 | 标准 | 不满足时的处理 |
|------|------|--------------|
| 所有采样点闭环极点 | max|lambda|_dyn < 0.99 | 增大 Q_theta，或对不稳定的腿长区间单独增大 Q |
| 最短/最长腿长的俯仰恢复时间比 | < 3x | 若差异过大，考虑分区 Q/R 调度而非仅 K 调度 |
| 拟合误差 | max relative RMS error < 5% | 提高多项式阶数或增加采样点密度 |
| 平衡偏移 fsolve 收敛 | 所有采样点收敛（ier=1） | 收紧 fsolve 的 xtol/ftol，或缩小工作范围 |
| K 矩阵连续性 | 相邻采样点的 K 变化平滑 | 若出现跳变，检查 fsolve 是否落入错误分支 |


---

## 8. 分阶段整定路线图

整定从内层到外层、从静态到动态、从低增益到高增益，严格按序执行。

```
Phase 0: 传感器 + 运动学验证
  确认 FK/IK 正确、IMU 读数正确、轮速无跳变
  |
Phase 1: 标称腿长静态平衡（LQR 仅姿态通道）
  腿长固定标称值，target_x=0, target_x_dot=0
  仅调 Q_theta + R_hip -> 无振荡稳定站立
  |
Phase 2: 姿态阻尼整形
  增调 Q_dtheta + Q_dleg -> 外部扰动后姿态恢复平滑
  |
Phase 3: 腿长 PID
  增调 Kp_pos, Kp_vel -> 腿长可伸缩，伸缩过程姿态稳定
  |
Phase 4: 位移/速度/偏航控制
  增调 Q_x + Q_xdot + Q_psi + Q_dpsi
  -> 可恒速前进/偏航，松手后刹车不振荡
  |
Phase 5: 增益调度
  全腿长范围扫描 -> 必要时 Q/R 分区间调度
  |
Phase 6: 动态工况
  飞坡、斜坡、外部推搡 -> 必要时引入额外补偿
```

**每阶段验收测试：**

| 阶段 | 验收测试 | 通过标准 |
|------|---------|---------|
| 1 | 初始 theta=0.05 rad，无控制输入 | 0.5s 内收敛站立，无振荡 |
| 2 | 水平脉冲力（如 20N, 0.1s） | 0.8s 内恢复，超调 < 20% |
| 3 | 腿长 0.20 <-> 0.28 <-> 0.16m 切换 | 伸缩过程机身不跳起，姿态无明显抖动 |
| 4 | 恒速 0.5 m/s 前进 5s | 位移误差 < 0.1m，速度超调 < 20% |
| 5 | 最短/最长腿长俯仰恢复时间比 | < 3x |
| 6 | 松手刹车（从 max_speed 到 0） | 无回退，刹车距离可接受 |

### 实机移植的保守启动

实机首次运行的 Q_DIAG 取仿真验证值的 0.3-0.5 倍，R_MAT 取仿真值的 3-5 倍（形成"软"控制器）。按 Phase 1-2 的流程在实机上逐步减小 R，每次减 20-30%，直到出现可感知的机身微振，回退一步作为安全上界。


---

## 9. K 矩阵诊断与交叉验证

K 矩阵的质量决定了闭环性能的上限。在整定开始前和每次大幅修改 Q/R 后，应执行以下诊断。

### 9.1 闭环极点检查

```python
import numpy as np
from scipy.linalg import eigvals

# 加载 A_d, B_d, K
d = np.load('AB_sampling_points.npz')
idx = np.argmin(np.abs(d['L_l'] - L) + np.abs(d['L_r'] - L))
A_cl = d['A_d'][idx] - d['B_d'][idx] @ d['K'][idx]

# 全部极点
evals = np.sort(np.abs(eigvals(A_cl)))[::-1]
print('All |eig|:', evals[:6])

# 排除积分器后的动态极点
# 积分器特征值对应的特征向量在 x（状态0）或 psi（状态1）分量上占主导 (>50%)
_, evecs = np.linalg.eig(A_cl)
for j in range(len(evals)):
    w = np.abs(evecs[:, j])
    is_integrator = w[0] > 0.5 or w[1] > 0.5
    print(f'  |eig[{j}]|={evals[j]:.4f}  integrator={is_integrator}  '
          f'top_states={np.argsort(w)[::-1][:3]}')
```

**检查标准：**
- 所有特征值 < 1.0（离散系统稳定性充要条件）
- 排除 x/psi 积分器后，最大动态极点 < 0.97
- 积分器特征值通常接近 1.0（0.995-0.999），这是正常的——它们对应位置/偏航的纯积分动力学，由速度指令驱动而不影响稳定性

### 9.2 A/B 矩阵物理一致性检查

线性化得到的 (A_d, B_d) 必须满足物理对称性和符号约束：

**对称性检查**（L_l = L_r 时，左右腿动力学应对称）：
```python
# 腿自耦合应对称
assert abs(Ad[8,3] - Ad[9,4]) < 1e-4
# 腿交叉耦合应对称
assert abs(Ad[9,3] - Ad[8,4]) < 1e-4
# yaw 耦合应反称
assert abs(Ad[6,3] + Ad[6,4]) < 1e-4
```

**A[5,3:5] 符号检查**（腿角 -> x 加速度耦合，必须为负）：
```python
assert Ad[5,3] < 0 and Ad[5,4] < 0, "A[5,3:5] must be negative!"
```
物理原因：腿前倾 -> 身体相对地面后移 -> x 加速度为负（牛顿第三定律）。若 A[5,3:5] 为正，说明线性化的动力学模型存在符号错误——需检查 Lagrangian 动能表达式是否正确包含了身体水平运动与腿角/俯仰角速度的交叉耦合项。

**B 矩阵输入耦合检查**：
- B[5,2:4]（轮力矩 -> x 加速度）应为正
- B[7,0:2]（髋力矩 -> 俯仰加速度）应为非零
- B[7,2:4]（轮力矩 -> 俯仰加速度）应为零（轮力矩不直接产生俯仰力矩）

### 9.3 跨模型交叉验证

如果你有来自其他已验证平台（如参考开源项目）的正确 A/B 矩阵，可以用于检查自己的线性化结果。虽然物理参数不同导致数值不同，但关键元素的**符号**和**相对量级关系**应一致：

```python
# 加载参考 K（来自 sys_matrices.mat 或已知正确的 .npz）
K_ref = ...  # 4x10 参考增益
K_ours = ... # 4x10 我们的增益

# 符号一致性：每列的关键符号应匹配
for col, name in enumerate(['x','psi','theta','thL','thR','xdot','psidot','thdot','thLdot','thRdot']):
    signs_ref = np.sign(K_ref[:, col])
    signs_ours = np.sign(K_ours[:, col])
    if not np.array_equal(signs_ref, signs_ours):
        print(f'WARNING: K[:,{col}] ({name}) sign mismatch!')

# 量级比检查：K[:,2] (pitch) 的腿力矩 vs 轮力矩分配比应接近
ratio_ref = abs(K_ref[0,2]) / (abs(K_ref[2,2]) + 1e-9)
ratio_ours = abs(K_ours[0,2]) / (abs(K_ours[2,2]) + 1e-9)
print(f'Pitch leg/wheel ratio: ref={ratio_ref:.1f}, ours={ratio_ours:.1f}')
```


---

## 10. 常见异常与排查

| 异常现象 | 可能原因 | 排查顺序 |
|---------|---------|---------|
| 仿真启动后立即发散 | K 矩阵符号反了（正反馈） | 1. 检查 u = -K*(x-x_ref) 的负号 2. 检查 A/B 符号（§9.2） 3. 检查 DARE 求解是否收敛 |
| 站立时持续小幅振荡 | 俯仰阻尼不足 | 增大 Q_dtheta，每次 +100% |
| 松手后刹车距离过长 | K[:,5] 速度增益过小 | 1. 增大 Q_xdot（2-10x） 2. 降低 R_wheel 3. 提高轮力矩限幅 |
| 停住后缓慢回退 | 位置目标冻结在松手前 | 松手瞬间 snap position_target = current_position |
| 前进时俯仰角大幅前倾 | Q_xdot 过大，为跟踪速度过度倾斜 | 降低 Q_xdot 或增大 Q_theta |
| 飞坡压缩量小，落地硬 | PID Kp_pos + Q_leg 过刚 | 1. 降 Kp_pos 2. 降 Q_leg 3. 降力限幅 |
| 加速时身体不倾斜 | R_hip/R_wheel 比值过大 | 降低 R_hip 或增大 R_wheel |
| 改了 Q 重生成 K，跑起来无变化 | 线性化输出写到了错误目录 | 确认在正确的工作目录运行线性化工具，确认生成的文件覆盖了运行时加载的文件 |
| 左右不对称（对称腿长下） | 线性化中的 Lagrangian 存在不对称项 | 检查身体动能表达式是否对称，检查 LegPoseSolver 是否正确处理左右腿几何 |
| 轮子打滑 | 轮力矩指令超过静摩擦 | 1. 降低 R_wheel 2. 降低前馈 gain 3. 检查轮地摩擦参数 |
| 腿长无法跟踪目标 | Kp_pos 不够或力限幅过低 | 1. 增大 Kp_pos 2. 增大力限幅 |
| 腿伸缩时机身弹跳 | Kp_pos 过大 | 降低 Kp_pos，增大 Kp_vel |
| Kalman 速度估计滞后 | q_vel/r_vel 比值过小 | 增大 q_vel，每次 x2 |
| DARE 迭代不收敛 | Ad 有接近单位圆特征值 | 1. 检查 A 矩阵是否有异常值 2. 换用 Schur 分解法（solve_discrete_are） |
| 增益调度某些腿长点不稳定 | 线性化在该点不可信 | 1. 检查 fsolve 收敛状态 2. 增加采样密度 3. 验证该点的 A_d 特征值是否合理 |
