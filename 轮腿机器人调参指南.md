# 轮腿机器人调参指南

## 10 维 LQR + PID 力控框架参数整定手册

**编写说明：**

本文档是《轮腿机器人控制理论手册》与《轮腿机器人高级控制与观测方法》的配套工程文档，聚焦于参数整定的**操作流程、数值计算与验证闭环**。理论与公式推导详见前述两份文档，本文不再重复。

本文档假设读者已完成以下前置工作：
- 机械参数（质量、惯量、杆长、轮径）已通过 CAD 模型或实测确定
- 五连杆运动学正解/逆解及雅可比矩阵已验证正确
- Kalman 滤波器已完成传感器噪声标定（或使用 MuJoCo 真值）

**安全声明：**

本文档给出的初始值计算方法和整定流程仅作为设计起点。所有参数必须在仿真中充分验证后方可移植至实机，实机首次测试应从最低增益开始，逐步递增。

---

## 目录

1. [参数体系总览](#1)
2. [LQR Q/R 权重矩阵](#2)
   - [2.1 参数含义](#2.1)
   - [2.2 初始值计算——Brysons 法则](#2.2)
   - [2.3 各状态权重的独立调节逻辑](#2.3)
   - [2.4 整定流程](#2.4)
   - [2.5 验证标准](#2.5)
3. [PID 力控参数](#3)
   - [3.1 腿长位置-速度级联 PID](#3.1)
   - [3.2 Roll 稳定 PID](#3.2)
4. [轮毂力矩前馈参数](#4)
   - [4.1 参数含义与初始值计算](#4.1)
   - [4.2 整定流程](#4.2)
5. [参考调速器参数](#5)
6. [安全限幅参数](#6)
   - [6.1 倾倒保护角](#6.1)
   - [6.2 力矩限幅](#6.2)
   - [6.3 力限幅](#6.3)
7. [Kalman 滤波器噪声参数](#7)
8. [线性化与增益调度工具链参数](#8)
9. [分阶段整定路线图](#9)
10. [常见异常与排查表](#10)

---

## <a id="1"></a>1. 参数体系总览

本系统的可调参数分布于四个层级：

```
+-------------------------------------------------------+
| 离线工具链 (tools/linearize.py)                        |
|  Q_DIAG[10]  R_MAT[4x4]  DT  H_MIN/H_MAX  D_d[5]    |
|  -> 输出: K 增益表 (data/lqr_gain_fit.py)              |
|  -> 输出: 平衡偏移 e2(L), e3(L)                        |
+--------------------------+----------------------------+
                           | 离线生成，在线查表
+--------------------------v----------------------------+
| 在线仿真 (config/robot_params.yaml -> controller.py)   |
|                                                        |
|  力控层: leg_length_pid  roll_pid                      |
|  前馈层: lqr_feedforward                               |
|  规划层: keyboard_command  lqr_target                  |
|  安全层: fall_angle_rad  *_force_limit  *_torque_limit |
|  估计层: kalman                                        |
+-------------------------------------------------------+
```

**参数变更的生效路径：**

| 参数位置 | 变更后需要 | 生效方式 |
|---------|-----------|---------|
| `tools/linearize.py` 中的 Q_DIAG/R_MAT/D_d | 重新运行 `python tools/linearize.py` | 生成新的 K 增益表和 e2/e3 系数，在线查表自动生效 |
| `tools/linearize.py` 中的 DT/H_MIN/H_MAX | 重新运行线性化 | 影响 A_d/B_d 离散化精度和增益调度范围 |
| `config/robot_params.yaml` 中除 lqr_target 外的参数 | 重新启动仿真 | 在线读取，实时生效 |
| `config/robot_params.yaml` 中的 lqr_target | 无需重启 | 通过键盘/调速器在线修改 |
| 命令行参数（--push-*, --joint-torque-limit 等） | 重新启动仿真 | 覆盖 yaml 中的对应默认值 |

**各层级参数间的耦合关系：**

- Q_DIAG 和 R_MAT 决定了 K 矩阵的所有 40 个元素，K 的幅值决定了控制力矩的大小，力矩是否触及限幅由安全层参数决定
- 腿长 PID 的 Kp 过大，腿伸缩过快，质心高度快速变化，俯仰通道的动力学参数（有效惯量、重力刚度）快速变化，LQR 的线性化假设被违背
- Kalman 的 q_vel 过大，速度估计噪声放大，LQR 的 x_dot 通道反馈含高频噪声，轮毂力矩抖动，轮地接触力波动
- 轮毂力矩前馈的 gain 过大，轮子加速度超过地面摩擦力上限，打滑，Kalman 里程计失效，位移估计漂移

---

## <a id="2"></a>2. LQR Q/R 权重矩阵

**文件**：`tools/linearize.py`，变量 `Q_DIAG`（10 维向量）和 `R_MAT`（4x4 矩阵）

**作用**：Q/R 通过离散代数 Riccati 方程（DARE）决定反馈增益矩阵 K 的全部 40 个元素。K 矩阵将 10 维状态偏差映射为 4 维控制指令 `u = -K * (x - x_target)`。Q 和 R 的相对比值决定控制性能，同比例缩放不影响 K。

### <a id="2.1"></a>2.1 参数含义

**Q_DIAG 各分量的物理含义**：

| 下标 | 状态 | 物理量纲 | Q 值增大的效果 |
|------|------|---------|---------------|
| 0 | x（水平位移） | m | 位移跟踪更紧，但可能引起速度超调 |
| 1 | psi（偏航角） | rad | 偏航跟踪更紧 |
| 2 | theta（机体俯仰角） | rad | 俯仰恢复更快，是稳定性最关键的元素 |
| 3 | theta_L（左腿摆角） | rad | 左腿角度偏差被更快纠正 |
| 4 | theta_R（右腿摆角） | rad | 右腿角度偏差被更快纠正 |
| 5 | x_dot（水平速度） | m/s | 速度阻尼增强，抑制位移超调和振荡 |
| 6 | psi_dot（偏航角速度） | rad/s | 偏航阻尼增强 |
| 7 | theta_dot（俯仰角速度） | rad/s | 俯仰阻尼增强，抑制俯仰振荡 |
| 8 | theta_L_dot（左腿摆角速度） | rad/s | 左腿运动阻尼增强 |
| 9 | theta_R_dot（右腿摆角速度） | rad/s | 右腿运动阻尼增强 |

Q 矩阵形式：Q = diag(Q_DIAG[0], ..., Q_DIAG[9])，仅对角元非零。非对角元保持为零，因为各状态的"最大可接受偏差"之间不存在物理意义上的联合约束。

**R_MAT 各分量的物理含义**：

| 下标 | 控制量 | 物理量纲 | R 值增大的效果 |
|------|--------|---------|---------------|
| 0,0 | tau_L（左髋虚拟力矩） | Nm | LQR 减少对左髋力矩的使用，左腿响应变慢 |
| 1,1 | tau_R（右髋虚拟力矩） | Nm | LQR 减少对右髋力矩的使用，右腿响应变慢 |
| 2,2 | T_wL（左轮毂力矩） | Nm | LQR 减少对左轮毂的使用，位移/偏航响应变慢 |
| 3,3 | T_wR（右轮毂力矩） | Nm | LQR 减少对右轮毂的使用，位移/偏航响应变慢 |

当前 R_MAT 取对角形式 diag(0.8, 0.8, 1.0, 1.0)，非对角元为零。非对角元非零意味着对两个控制通道的"联合使用"施加惩罚——如不希望 tau_L 和 tau_R 同向大幅出力时。常规工况下无需此约束。

### <a id="2.2"></a>2.2 初始值计算——Brysons 法则

**原理**：将不同物理量纲的状态量和控制量归一化为无量纲的"偏差比例"：

$$
Q_{ii}^{(0)} = \frac{1}{(\Delta x_i^{\max})^2}, \quad
R_{jj}^{(0)} = \frac{1}{(\tau_j^{\max})^2}
$$

其中 $\Delta x_i^{\max}$ 是第 i 个状态的最大可接受偏差（超出此值系统进入不可接受状态），$\tau_j^{\max}$ 是第 j 个控制通道可用的最大力矩。

**计算步骤**：

Step 1——确定各状态的最大可接受偏差：

| 状态 | 确定方法 | 典型值参考 |
|------|---------|-----------|
| theta（俯仰角） | 取物理倾倒角（质心超出支撑域对应的角度）的 1/5 ~ 1/3 | 倾倒角约 20-30°，取 4-6°（0.07-0.10 rad），则 Q_theta = 100-200 |
| theta_L/R（腿摆角） | 取腿工作空间摆角范围的 1/5 | 工作范围约 30°，取 6°（0.10 rad），则 Q_leg = 100 |
| x（水平位移） | 取允许的最大稳态跟踪误差 | 0.3 m，则 Q_x = 11 |
| psi（偏航角） | 取允许的最大航向偏差 | 0.4 rad，则 Q_psi = 6 |
| 所有速度量 | 不遵循 Brysons——见 §2.3 的独立逻辑 | — |

Step 2——确定各控制通道的最大可用力矩：

| 控制量 | 确定方法 | 典型值参考 |
|--------|---------|-----------|
| tau_L/R（髋力矩） | 取执行器力矩限幅的 30%-50%（Riccati 方程中的"设计限幅"应低于硬限幅） | 硬限幅 40 Nm，取 15 Nm，则 R_hip = 1/225 = 0.0044 |
| T_wL/R（轮毂力矩） | 取轮毂电机力矩限幅的 30%-50% | 硬限幅 5 Nm，取 2 Nm，则 R_wheel = 1/4 = 0.25 |

**注意**：Brysons 法则给出的 R_hip（0.0044）远小于当前代码中的值（0.8）。这是因为代码中的 R 并非直接取 Brysons 数值——Brysons 法则给出的是"量纲归一化"的起点，实际工程中 R 需要在此基础上乘以一个全局缩放因子来调节整体控制激进程度。代码中 Q/R 的**比值**才是决定 K 矩阵的核心——Q_theta / R_hip = 15000/0.8 = 18750 与 Brysons 推导（200/0.0044 = 45454）在同一数量级。

### <a id="2.3"></a>2.3 各状态权重的独立调节逻辑

**位置权重的调节**：

- **俯仰角 theta**：压倒性最高优先级。俯仰角是不稳定模态（A 矩阵有正实部特征值），如果 Q_theta 不够大，闭环极点可能无法被充分吸引到左半平面。调节目标：时域阶跃响应中，俯仰角的恢复时间应在 0.3-0.8 s 内（对应闭环带宽 15-25 rad/s）
- **腿摆角 theta_L/R**：次高优先级。腿摆角变化直接移动质心，间接影响俯仰稳定性。调节目标：腿摆角不出现独立于俯仰角的高频甩动
- **水平位移 x**：低优先级。位移是欠驱动通道——LQR 无法直接施加位移控制力，只能通过俯仰角偏转间接产生水平加速度。Q_x 的主要作用是提供一个"目标方向"，让 LQR 在满足俯仰稳定性的约束下尽量减小位移偏差。Q_x 取过大会导致 K 矩阵中 x 相关元素过大，LQR 为追求位移收敛而过度倾斜机身
- **偏航角 psi**：低优先级。偏航通道与俯仰通道近似解耦，偏航控制在姿控稳定后再调整

**速度权重的调节（阻尼整形）**：

速度权重不遵循 Brysons 法则。它们的作用是通过 Riccati 方程中的交叉项影响各模态的阻尼比。调节逻辑为：

- 俯仰角速度 theta_dot：Q_dtheta 决定俯仰模态的阻尼比。从 0 开始逐步增大：0 -> 出现等幅振荡 -> 继续增大 -> 振荡衰减 -> 进一步增大 -> 过阻尼（响应迟钝）。最佳值取刚好消除超调的超调阻尼点（临界阻尼附近，zeta = 0.7-0.9），对应 Q_dtheta / Q_theta 比值约 0.0005-0.005
- 腿摆角速度 theta_L/R_dot：主要为抑制腿在高增益位置反馈下的高频抖动。初值取 Q_dtheta 的 0.5-1 倍
- 水平速度 x_dot：抑制速度超调，防止位移跟踪中的来回振荡。初值取 Q_x 的 5-20 倍
- 偏航角速度 psi_dot：抑制偏航振荡。初值取 Q_psi 的 1-5 倍

**R 矩阵内部分配的调节**：

```
R 的内部结构: diag(R_hip, R_hip, R_wheel, R_wheel)
```

- R_hip/R_wheel 的比值决定了 LQR 对髋关节力矩 vs 轮毂力矩的偏好。比值 < 1（R_hip 更小）意味着 LQR 更倾向于使用髋关节力矩——这在姿态恢复中更有效但也更容易引起腿摆动。比值 > 1 表示更依赖轮毂力矩
- 实机上 R_hip 通常小于 R_wheel，因为关节电机的力矩容量（40 Nm）远大于轮毂电机（5 Nm），且轮毂力矩过大会导致打滑
- 左右腿 R 值通常取相等，除非机器人存在显著的非对称性（如单侧负载）需要差异化分配

### <a id="2.4"></a>2.4 整定流程

整定顺序严格按以下步骤执行，每步确认无误后再进入下一步：

**Step 1：单一标称腿长（L=0.194m），静态验证**

运行 `python tools/linearize.py` 生成 K 矩阵。输出末尾会自动打印两项诊断：

```
Closed-loop max|lambda|: 0.XXXX  (worst at L_l=..., L_r=...)
K matrix |element| range: min=...  max=...  median=...
```

检查标准：
- `Closed-loop max|lambda|` < 0.99（必须）。若 >= 1.0 则闭环不稳定——K 矩阵无法镇定当前 (A_d, B_d)，需增大 Q[2]（theta）或减小 R
- `K matrix max` < 5000。若超过此值，说明 Q/R 比值过大，某些通道的反馈增益过高——可能引起力矩限幅频繁触发和噪声放大
- `K matrix min` 不应为零。若某元素精确为零，检查对应状态的 Q 值是否为零

若需要查看单个腿长点的详细诊断，可在 Python 交互环境中：
```python
import numpy as np
d = np.load('data/AB_sampling_points.npz')
# 找到标称腿长的索引
idx = np.argmin(np.abs(d['L_l'] - 0.194) + np.abs(d['L_r'] - 0.194))
A_d, B_d, K = d['A_d'][idx], d['B_d'][idx], d['K'][idx]
eig_vals = np.linalg.eigvals(A_d - B_d @ K)
print('max|lambda| =', np.max(np.abs(eig_vals)))
print('K range:', K.min(), K.max())
```

**Step 2：单一标称腿长，初始俯仰角偏差测试**

固定腿长，给初始俯仰角偏差 0.05 rad（约 3°），不加外部扰动，记录俯仰角恢复曲线：

| 现象 | 诊断 | 调整 |
|------|------|------|
| 单调收敛，0.3-0.8s 回到零 | OK | 进入 Step 3 |
| 多次超调振荡 | 俯仰阻尼不足 | 增大 Q[7]（theta_dot），每次 +50%，直到超调消失 |
| 恢复极慢（>1s） | 俯仰反馈强度不足 | 增大 Q[2]（theta），每次 +50%，或减小 R[0],R[1]，每次 -30% |
| 俯仰恢复快但腿摆角大幅甩动 | 俯仰-腿交叉耦合增益过大 | 增大 Q[8],Q[9]（腿摆角速度），每次 +100% |

**Step 3：单一标称腿长，水平脉冲扰动测试**

通过 `--push-force-x` 施加 0.1s 的水平脉冲力（如 20N），观察：

| 现象 | 诊断 | 调整 |
|------|------|------|
| 扰动后快速恢复，无振荡 | OK | 进入 Step 4 |
| 俯仰角长时间不回零，存在稳态偏置 | 平衡偏移补偿 e2(L) 不正确 | 重新运行 linearize.py 确认 e2 拟合精度 |
| 轮子打滑，机身漂移 | 轮毂力矩响应过大 | 增大 R[2],R[3] 或降低轮毂力矩前馈 gain |
| 腿长剧烈伸缩 | 位置环 Kp 过大或 Q_theta_L 不够 | 先检查 PID Kp（§3），再调整 Q[3],Q[4] |

**Step 4：增益调度全范围扫描**

对 H_MIN 到 H_MAX 的所有采样点重复 Step 1-2，确保：
- 所有腿长下闭环极点稳定
- 最短腿长（L=0.106m，最稳定）和最长腿长（L=0.366m，最不稳定）的俯仰恢复时间差异不超过 3x
- 若长腿时稳定性显著恶化，需要不同腿长区间使用不同的 Q/R（Q/R 增益调度），而非仅 K 调度

**Step 5：速度跟踪测试**

通过键盘指令给目标速度（0.2 -> 0.5 -> 1.0 m/s 阶梯），观察：
- 速度跟踪稳态误差 < 0.05 m/s
- 加减速过程中俯仰角波动 < 0.05 rad
- 若加减速时俯仰角剧烈前倾/后仰，增大 Q[5]（x_dot）或调整轮毂力矩前馈参数（§4）

**Step 6：实机移植的保守启动**

实机首次运行时的 Q_DIAG 取仿真验证值的 0.3-0.5 倍，R_MAT 取仿真值的 3-5 倍（保守"软"控制）。按 Step 2 的流程在实机上逐步减小 R，每次减 20-30%，直到出现可感知的机身微振，回退一步作为安全上界。

### <a id="2.5"></a>2.5 验证标准

以下指标在标称腿长、无外部扰动条件下测量：

| 项目 | 标准 | 验证方法 |
|------|------|---------|
| 闭环稳定性 | max|lambda(A_d-B_d K)| < 0.99 | 运行 `python tools/linearize.py`，输出中 `Closed-loop max|lambda|` 行 |
| 俯仰角阶跃恢复时间 | < 0.8 s（回到峰值的 5% 以内） | 初始 theta=0.05 rad，时域记录 |
| 俯仰角超调量 | < 10%（0.8 s 后无二次穿越） | 同上 |
| 位移跟踪稳态误差 | < 0.05 m | 恒速 0.5 m/s 指令下，测量 body_x 与 target_x 之差 |
| K 矩阵元素量级 | 所有元素绝对值 < 5000 | 运行 `python tools/linearize.py`，输出中 `K matrix |element| range` 行 |
| 力矩 RMS（仿真 10s 无扰动） | hip < 15 Nm, wheel < 2 Nm | 积分力矩平方/时长

---

## <a id="3"></a>3. PID 力控参数

**文件**：`config/robot_params.yaml`，路径 `control_initial.leg_length_pid` 和 `control_initial.roll_pid`

**作用**：PID 力控负责在任务空间（虚拟腿长 L、Roll 角）产生轴向力指令。PID 输出与 LQR 输出通过 VMC 力矩映射合成为关节力矩。PID 和 LQR 的分工原则：PID 处理可解耦的单通道任务（腿长伸缩、Roll 稳定），LQR 处理全耦合的多通道协调（姿态平衡 + 位移跟踪）。

### <a id="3.1"></a>3.1 腿长位置-速度级联 PID

**结构**：外环（位置 P）+ 内环（速度 P），无积分项。等效为：

```
F_pos = Kp_pos * (L_target - L_measured)
F_vel = Kp_vel * (0 - L_dot_measured)
F_leg = F_pos + F_vel + G_m*重力补偿
```

不使用积分项（Ki=0）的原因：轮腿机器人的腿长变化频繁（地形适应、跨越台阶），积分器的记忆效应会在腿长切换时引起过冲和滞后。稳态精度由 LQR 的位移积分效应（通过俯仰偏置间接实现）和重力前馈补偿来保证。

**参数含义**：

| 参数 | 位置 | 当前值 | 物理含义 | 增大效果 | 上限约束 |
|------|------|--------|---------|---------|---------|
| position_kp | leg_length_pid | 2000 | 腿长偏差 1m 产生 2000N 的轴向力 | 腿长跟踪更紧，伸缩更快 | 过大导致腿伸缩时质心加速度超过轮地摩擦力上限 -> 打滑 |
| position_ki | leg_length_pid | 0 | — | — | 不使用 |
| position_kd | leg_length_pid | 0 | — | — | 不使用 |
| velocity_kp | leg_length_pid | 100 | 腿伸缩速度 1m/s 产生 100N 的阻尼力 | 伸缩运动更平滑，抑制振荡 | 过大导致腿伸缩阻力过大 -> 地面冲击时腿无法缓冲 |
| velocity_ki/kd | leg_length_pid | 0 | — | — | 不使用 |

**初始值计算**：

- position_kp：从静态承载出发——腿长偏差 1cm 应产生足够额外力来加速腿部质量。`Kp_pos = m_leg * g / 0.01 = 1.2*9.81/0.01 ≈ 1200`。取 1.5-2x 安全因子 -> 2000
- velocity_kp：取临界阻尼 `Kp_vel = 2 * sqrt(Kp_pos * m_leg) * zeta`，zeta=0.7 -> `2 * sqrt(2000*1.2) * 0.7 ≈ 68`。向上取整到 100

**整定流程**（在 LQR 已稳定后执行）：

1. 关闭 LQR（将 K 矩阵临时置零），仅保留 PID + 重力补偿
2. 调 position_kp：从 500 开始，每次 +500，直到腿长阶跃响应（目标腿长从 0.194 跳到 0.22m）在 0.3s 内完成且无明显超调
3. 调 velocity_kp：从 50 开始，每次 +50，直到腿伸缩过程平滑无振荡
4. 开启 LQR，检查 PID 与 LQR 的交互——若腿长频繁振荡，降低 position_kp 20-30%

### <a id="3.2"></a>3.2 Roll 稳定 PID

**结构**：PD（Kp + Kd），无积分：

```
F_roll = Kp_roll * (0 - roll_angle) + Kd_roll * (0 - roll_rate)
```

Roll 方向是自稳定的（重力提供恢复力矩），PID 的主要作用是提供额外阻尼和加快恢复速度。不加积分的原因同腿长 PID。

| 参数 | 当前值 | 增大效果 | 上限约束 |
|------|--------|---------|---------|
| kp | 2000 | Roll 恢复更快 | 过大导致左右腿轴向力频繁跳变 -> 机身左右摇晃 |
| kd | 1000 | Roll 阻尼更大 | 过大导致 Roll 响应迟钝 |
| ki | 0 | — | 不使用 |

**整定方法**（在 LQR 已稳定后执行）：

1. Kp=0，Kd=0，施加初始 Roll 角 0.05 rad，记录自然恢复的振荡频率 f_roll（约 2-4 Hz）
2. Kp = I_roll * (2*pi*f_roll)^2，其中 I_roll 为绕 x 轴的有效转动惯量（粗略取 total_mass * (L/2)^2 ≈ 0.17 kg*m^2）。Kp = 0.17 * (2*3.14*3)^2 ≈ 60。取 2000 是因为模型简化误差大，需要大幅放大
3. Kd = 2 * sqrt(Kp * I_roll) * zeta，zeta=0.7。Kd = 2 * sqrt(2000 * 0.17) * 0.7 ≈ 26。取 1000 是因为实际系统需要比刚体模型更强的阻尼来抑制高频振动

---

## <a id="4"></a>4. 轮毂力矩前馈参数

**文件**：`config/robot_params.yaml`，路径 `control_initial.lqr_feedforward`

**作用**：利用参考调速器输出的目标加速度和速度误差，直接计算轮毂力矩补偿量，叠加到 LQR 输出上。这等价于一个基于参考模型的前馈通道——在 LQR 反馈尚未感知到速度偏差时预先施加力矩，减小速度跟踪的相位滞后。

### <a id="4.1"></a>4.1 参数含义与初始值计算

| 参数 | 当前值 | 物理含义 | 计算公式 |
|------|--------|---------|---------|
| x_accel_to_wheel_torque_Nm_per_mps2 | 5.0 | 目标加速度 1 m/s^2 对应单个轮毂力矩 Nm | total_mass * wheel_radius / 2 = 18.16*0.05/2 = 0.454（理论值）。实际取 5.0 是考虑传动损耗和模型简化误差 |
| x_decel_to_wheel_torque_Nm_per_mps2 | 5.0 | 同上，制动时使用 | 与加速取相同值。若制动打滑可适当降低 |
| x_velocity_error_to_wheel_torque_Nm_per_mps | 1.0 | 速度误差 1 m/s 对应轮毂力矩 Nm | P 型速度前馈增益。初值取 1.0，根据速度跟踪稳态误差调整 |
| x_brake_velocity_error_to_wheel_torque_Nm_per_mps | 1.0 | 同上，制动时使用 | 与加速取相同值 |
| max_wheel_torque_ff_Nm | 2.0 | 前馈力矩的上限 | 取轮毂电机力矩限幅 5 Nm 的 40%——留 60% 给 LQR 反馈通道 |
| max_brake_wheel_torque_ff_Nm | 2.0 | 同上，制动时 | 与加速取相同值 |

**理论值与工程值的差异**：x_accel_to_wheel_torque 的理论值来自刚体运动学 `T = m * a * R / 2`（每条腿承担一半质量的加速力矩）。实际取 5.0 远超理论值 0.454——这表明当前的前馈参数更接近一个"高增益 P 型速度前馈"而非严格的物理模型前馈。在实机上应逐步降低到接近理论值，依赖于 LQR 反馈来弥补速度跟踪误差，避免前馈过饱和。

### <a id="4.2"></a>4.2 整定流程

1. 将加速度前馈 gain 和速度前馈 gain 全部置零
2. 运行恒速跟踪测试（0.5 m/s），记录纯 LQR 下的速度跟踪稳态误差 e_ss
3. 逐步增大 x_velocity_error_to_wheel_torque：每次 +0.5，直到稳态误差减小到 0.05 m/s 以内或出现速度振荡
4. 运行变加速测试（加速度 ramp 0 -> 1.0 m/s^2），记录加速度跟踪滞后
5. 逐步增大 x_accel_to_wheel_torque：从理论值 0.45 开始，每次 +1.0，直到加速度跟踪滞后显著减小或轮子出现打滑迹象
6. 设置 max_wheel_torque_ff_Nm = 当前前馈力矩在最大加速度工况下的峰值 * 1.2

---

## <a id="5"></a>5. 参考调速器参数

**文件**：`config/robot_params.yaml`，路径 `control_initial.keyboard_command`

**作用**：将键盘输入（-1.0 ~ +1.0）映射为目标速度/偏航速率，并通过斜率限制（ramp rate limit）生成平滑的参考轨迹。LQR 的目标状态（target_x_dot, target_yaw_dot, target_x_ddot）全部来自调速器的输出。

| 参数 | 当前值 | 含义 | 整定方法 |
|------|--------|------|---------|
| max_speed_mps | 2.0 | 满油门对应的最大目标速度 | 取机器人实测安全最高速度的 70% |
| max_yaw_rate_radps | 2.0 | 满偏航指令对应的最大偏航速率 | 从 0.5 开始每次 +0.5，测试原地旋转稳定性，出现明显甩尾时回退 |
| speed_accel_mps2 | 5.0 | 加速时的斜率限制 | 取轮地最大静摩擦加速度的 50%。mu*g = 0.8*9.81 = 7.85，取 5.0 |
| speed_release_accel_mps2 | 5.0 | 减速/松开时的斜率限制 | 与加速取相同值 |
| instant_stop_on_release | false | true: 松开方向键立即置零目标速度；false: 按 release_accel 渐降 | true 响应快但可能引起急刹导致前翻；false 更平滑。首次整定用 false |
| instant_yaw_rate_command | true | true: 偏航速率直接跟随键盘，无加加速度限制 | 偏航惯性小，可直接跟随 |

**注意**：max_speed_mps 取 2.0 是一个较高的目标值。仿真中可先用低速（0.5 m/s）整定 LQR 和 PID，待低速稳定后再逐步提高 max_speed_mps 来暴露增益不足的问题。

---

## <a id="6"></a>6. 安全限幅参数

### <a id="6.1"></a>6.1 倾倒保护角

**参数**：`fall_angle_rad`（当前值：1.0 rad ≈ 57°）

**含义**：俯仰角或 Roll 角的绝对值超过此阈值，仿真自动停止。

**整定**：取机器人质心投影超出支撑域边界的临界姿态角的 0.8 倍。在标称腿长（0.194m）下，质心高度约 0.25m，前后支撑半径约 0.1m，临界角 = atan(0.1/0.25) ≈ 0.38 rad。但轮腿机器人可通过腿的主动伸展扩大支撑域，实际临界角远大于此。取 1.0 rad 是一个较保守的安全值——在此角度下机器人的实际物理状态非常接近不可恢复的倾倒。

### <a id="6.2"></a>6.2 力矩限幅

| 参数 | 位置 | 当前值 | 依据 |
|------|------|--------|------|
| joint_torque_limit | 命令行 --joint-torque-limit | 40 Nm | 关节电机（如 T-Motor AK 系列）的额定力矩。需根据实际电机型号确定 |
| wheel_torque_limit | 命令行 --wheel-torque-limit | 5 Nm | M3508 轮毂电机的额定力矩。参考 DJI 官方手册 |

力矩限幅在控制链的最末端（`_map_to_actuators` 中的 `clip` 操作），是硬限幅。这与 R 矩阵的"软权重"不同：R 告诉 LQR 在优化中尽量不碰限幅；硬限幅在优化结果超出物理能力时强制截断。

### <a id="6.3"></a>6.3 力限幅

| 参数 | 命令行参数 | 当前值 | 限制对象 |
|------|-----------|--------|---------|
| length_position_force_limit | --length-position-force-limit | 1500 N | 腿长位置环输出的轴向力上限 |
| length_velocity_force_limit | --length-velocity-force-limit | 1000 N | 腿长速度环输出的阻尼力上限 |
| roll_force_limit | --roll-force-limit | 1000 N | Roll PID 输出的力差上限 |

这些限幅保护的是：腿长 PID 在极端偏差（如刚启动时目标腿长与实际腿长相差很大）下不会产生危险的大轴向力，导致关节力矩经 VMC 映射后超出电机能力。

---

## <a id="7"></a>7. Kalman 滤波器噪声参数

**文件**：`config/robot_params.yaml`，路径 `control_initial.kalman`

**作用**：2 维 Kalman 滤波器（位置 + 速度）融合轮式里程计和 IMU 加速度，产生 x 和 x_dot 的估计。过程噪声协方差 Q_cov 和测量噪声协方差 R_cov 的比值决定 Kalman 增益——比值越大，滤波器越信任测量（响应更快但噪声更大），反之更信任模型预测（更平滑但滞后更大）。

| 参数 | 当前值 | 含义 | 整定方法 |
|------|--------|------|---------|
| q_pos | 0.0001 | 位置过程噪声 (m^2) | 先保持默认值，仅调整 q_vel |
| q_vel | 0.01 | 速度过程噪声 (m^2/s^2) | 从 0.01 开始，每次 *2，直到速度估计的时域波形略显毛刺，回退一步 |
| r_pos | 1.0 | 里程计位置测量噪声 (m^2) | 实测：轮子静止时，记录 10s 的里程计位置读数的方差 |
| r_vel | 0.5 | 里程计速度测量噪声 (m^2/s^2) | 实测：恒速运转时，记录轮速读数的方差除以 dt^2 |

**整定优先级**：Kalman 参数的整定应在 LQR/PID 整定之前完成（或至少在整定过程中不频繁修改）。因为 Kalman 的输出是 LQR 的输入——Kalman 的速度估计噪声水平直接决定了 LQR 所能达到的"有效阻尼"上限。

---

## <a id="8"></a>8. 线性化与增益调度工具链参数

**文件**：`tools/linearize.py`

| 参数 | 当前值 | 含义 | 修改建议 |
|------|--------|------|---------|
| DT | 0.002 | ZOH 离散化步长 (s) | 与 MuJoCo timestep 和 control_period_s 保持一致 |
| H_MIN | 0.106 | 增益调度最小腿长 (m) | 取五连杆最小腿长 + 5mm 安全余量 |
| H_MAX | 0.366 | 增益调度最大腿长 (m) | 取五连杆最大腿长 - 5mm 安全余量 |
| N_ITER_DARE | 100000 | DARE 反向迭代次数 | 降低到 20000 可加速计算，但需验证收敛性：检查 norm(P_k - P_{k-1})/norm(P_{k-1}) < 1e-10 |
| D_d | diag(0.1, 0.1, 0.15, 0.06, 0.06) | 阻尼矩阵（A 矩阵右下角） | 各元素分别对应 x/psi/theta/theta_L/theta_R 的粘性阻尼。增大俯仰阻尼（第三个元素）可补偿未建模的关节摩擦和空气阻力。对 K 矩阵的影响通过 Riccati 方程间接传播——阻尼增大 -> A 的特征值左移 -> 等 Q/R 下反馈增益需求降低 |

**D_d 的整定逻辑**：D_d 是非线性动力学中速度相关项（科里奥利力、离心力、关节摩擦）在平衡点处的线性化残差。它的选取不追求精确物理建模——精确的 C(q,q_dot) 矩阵是非线性的——而是提供"合理的阻尼基底"防止 A 矩阵中速度通道过于无阻尼。DARE 求解后，LQR 的反馈增益会在此基础上补充额外的阻尼。

---

## <a id="9"></a>9. 分阶段整定路线图

整定顺序必须从内层到外层、从静态到动态、从低增益到高增益：

```
Phase 0: 传感器 + 运动学
  确认 FK/IK 正确、IMU 读数正确、轮速/编码器无跳变
  |
Phase 1: 静态平衡（LQR 仅姿态通道）
  腿长固定 0.194m，目标 x=0, x_dot=0
  仅调 Q[2](theta) + R[0],R[1] -> 无振荡稳定站立
  |
Phase 2: 姿态阻尼整形
  增调 Q[7](theta_dot) + Q[8],Q[9](theta_L/R_dot)
  -> 外部扰动后姿态恢复平滑
  |
Phase 3: 腿长 PID
  增调 leg_length_pid -> 腿长可伸缩，伸缩过程姿态稳定
  |
Phase 4: 位移速度控制
  增调 Q[0](x) + Q[5](x_dot) + Q[1](psi) + Q[6](psi_dot)
  -> 可恒速前进/偏航
  |
Phase 5: 轮毂前馈
  增调 lqr_feedforward -> 加速响应改善
  |
Phase 6: 增益调度
  全腿长范围扫描 -> 必要时 Q/R 分区间调度
  |
Phase 7: 动态工况
  斜坡、台阶、外部推搡等 -> 必要时引入 LESO 扰动补偿
```

每阶段完成后执行如下验收测试：

| 阶段 | 验收测试 |
|------|---------|
| 1 | 初始 theta=0.05 rad，无控制输入，0.5s 内收敛站立 |
| 2 | --push-force-x 20N 脉冲，0.8s 内恢复，超调 < 20% |
| 3 | 腿长从 0.194 <-> 0.25 <-> 0.15m 切换，伸缩过程机身不跳起 |
| 4 | 恒速 0.5 m/s 前进 5s，位移误差 < 0.1m |
| 5 | 0 -> 1.0 m/s 加速，加速度跟踪滞后 < 0.1s |
| 6 | 最短/最长腿长下俯仰恢复时间差异 < 3x |

---

## <a id="10"></a>10. 常见异常与排查表

| 异常现象 | 可能原因 | 排查顺序 |
|---------|---------|---------|
| 仿真启动后立即发散 | K 矩阵极性反了（反馈正反馈） | 1. 检查 A/B 矩阵线性化是否正确 2. 检查 dlqr 求解是否收敛 3. 检查 u = -K*(x-target) 的负号 |
| 站立时持续小幅振荡 | 俯仰阻尼不足 | 增大 Q[7]（theta_dot），每次 +50% |
| 前进时俯仰角大幅前倾 | Q[5]（x_dot）过大，LQR 为跟踪速度过度倾斜机身 | 降低 Q[5] 或增大 Q[2]（theta） |
| 轮子持续打滑 | 轮毂力矩指令过大 | 1. 降低 R[2],R[3] 2. 降低 max_wheel_torque_ff_Nm 3. 检查轮地摩擦参数 |
| 腿长无法跟踪目标 | position_kp 不够，或 length_position_force_limit 过低 | 1. 增大 position_kp 2. 增大力限幅 |
| 腿伸缩时机身弹跳 | position_kp 过大，伸缩速度超过轮地接触可维持的范围 | 降低 position_kp，增大 velocity_kp |
| Kalman 速度估计滞后明显 | q_vel/r_vel 比值过小 | 增大 q_vel，每次 *2 |
| 增益调度某些腿长点不稳定 | 线性化在该腿长处不可信（非平衡点附近的泰勒展开不准确） | 1. 检查 fsolve 在该腿长的收敛状态 2. 增加 H_MIN/H_MAX 范围内的采样点密度 |
| DARE 迭代不收敛 | A_d 有接近单位圆的特征值，或 Q/R 量级差异过大 | 1. 检查 A 矩阵是否有异常值 2. 尝试增大 N_ITER_DARE 3. 尝试使用 scipy.linalg.solve_discrete_are 替代迭代法 |

